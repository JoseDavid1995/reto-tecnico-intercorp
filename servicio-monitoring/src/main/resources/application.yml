server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://intercorp_mysql:3306/intercorp_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true